# WALLE
Detecting objects with computer vision and python3
First of all you need to make sure [Python 3.7+](https://www.python.org/downloads/) is installed and install ImageAI and OpenCV libraries
```
& pip install -r requirements.txt
```

